---
# vars file for payara
payara_download_base: "https://repo1.maven.org/maven2/fish/payara"

payara_user: payara
payara_group: payara
payara_base: /opt/payara
payara_home: "{{ payara_base }}/payara{{ payara_glassfish_version }}{{ payara_glassfish_release }}"
